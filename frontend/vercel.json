{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://gridbert-api.fly.dev/api/:path*" },
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
